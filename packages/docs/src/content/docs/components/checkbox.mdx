---
title: Checkbox
description: Multi-select boolean inputs.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.astro';
import { Checkbox } from '@nirman/components';

Checkboxes represent additive boolean selections.

## Interactive Playground

<Playground>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
    <Checkbox client:load label="Accept tracking cookies" defaultChecked />
    <Checkbox client:load label="I agree to the Terms of Service" />
    <Checkbox client:load label="Disabled option" disabled />
  </div>
</Playground>

<Tabs>
  <TabItem label="React Usage">
    ```tsx
    import { Checkbox } from '@nirman/components';

    <Checkbox label="Send me marketing emails" />
    ```
  </TabItem>
  <TabItem label="Android Compose Usage">
    ```kotlin
    Checkbox(
        checked = checkedState.value,
        onCheckedChange = { checkedState.value = it },
        colors = CheckboxDefaults.colors(
            checkedColor = Tokens.ColorPrimaryDefault
        )
    )
    ```
  </TabItem>
</Tabs>
