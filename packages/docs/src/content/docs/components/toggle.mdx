---
title: Toggle
description: Instant state boolean switches.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.astro';
import { Toggle } from '@nirman/components';

Toggles map natively to `color.primary.default` when active, and `interaction.disabled-bg` when inactive.

## Interactive Playground

<Playground>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem' }}>
    <Toggle client:load label="Enable Notifications" defaultChecked />
    <Toggle client:load label="Dark Mode" />
  </div>
</Playground>

<Tabs>
  <TabItem label="React Usage">
    ```tsx
    import { Toggle } from '@nirman/components';
    <Toggle label="Wi-Fi" />
    ```
  </TabItem>
</Tabs>
