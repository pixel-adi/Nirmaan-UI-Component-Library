---
title: Input
description: Layer 1 Element — Standard form text capture.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.astro';
import TokenMapping from '../../../components/TokenMapping.astro';
import { Input } from '@nirman/components';

Text inputs govern standard data entry. Every border, label, error colour, and spacing value is token-driven — no hardcoded values exist in the component implementation.

## 1. Accessibility Specification

| Attribute | Requirement | Detail |
| :--- | :--- | :--- |
| **Label** | Always visible `<label>` linked via `htmlFor`/`id` | Floating or fixed — never placeholder-only labels. |
| **Error** | `aria-invalid="true"` + `aria-describedby` pointing to error text | Screen readers announce "invalid entry" automatically. |
| **Error message** | `role="alert"` | Triggers immediate announcement on error state change. |
| **Helper text** | `aria-describedby` | Linked to input — announced after label. |
| **Required** | `aria-required="true"` | Announced as "required field". |
| **Focus ring** | 2px solid `--nir-colour-border-focus`, 0px offset (inside) | Inset outline to avoid layout shift. |
| **Touch target** | Minimum 44px height | Enforced via `min-height: var(--nir-spacing-touch-target)`. |
| **Disabled** | `aria-disabled="true"` + `cursor: not-allowed` | Not HTML `disabled` — keeps tab navigation intact. |

## 2. Token Binding

<TokenMapping tokens={[
  { element: 'border-color (default)', token: '--nir-color-border-default', value: '#e6e2de', description: 'cream-400 resting border.' },
  { element: 'border-color:hover', token: '--nir-color-border-strong', value: '#c8c4c0', description: 'cream-600 on hover.' },
  { element: 'border-color:focus', token: '--nir-color-primary-default', value: '#083459', description: 'navy-600 on keyboard focus. 2px inset outline.' },
  { element: 'border-color (error)', token: '--nir-color-feedback-error-default', value: '#bc3737', description: 'red-500 — automatic on error prop.' },
  { element: 'background', token: '--nir-color-surface-default', value: '#fef6ee', description: 'cream-50 default. container (#fcfcfc) inside cards.' },
  { element: 'color (text)', token: '--nir-color-text-default', value: '#010f2a', description: 'navy-900 — near-black for maximum legibility.' },
  { element: 'color (placeholder)', token: '--nir-color-text-placeholder', value: '#8298aa', description: 'navy-200 muted placeholder.' },
  { element: 'color (label)', token: '--nir-color-text-default', value: '#010f2a', description: 'Label inherits default text colour.' },
  { element: 'color (error text)', token: '--nir-color-feedback-error-text', value: '#bc3737', description: 'Matches border for visual coherence.' },
  { element: 'color (helper text)', token: '--nir-color-text-secondary', value: '#23313c', description: 'ink-600 — slightly muted below label.' },
  { element: 'border-radius', token: '--nir-radius-interactive', value: '6px', description: 'Consistent with all form controls.' },
  { element: 'padding-inline', token: '--nir-spacing-component-padding-md', value: '16px', description: '16px horizontal padding (md size).' },
  { element: 'min-height', token: '--nir-spacing-touch-target', value: '44px', description: 'WCAG 2.5.8 minimum.' },
  { element: 'font-size (label)', token: '--nir-font-size-sm', value: '14px', description: 'Smaller than body for visual hierarchy.' },
  { element: 'font-size (input)', token: '--nir-font-size-base', value: '16px', description: 'Prevents iOS auto-zoom (must be ≥16px).' },
  { element: 'background (disabled)', token: '--nir-color-interaction-disabled-bg', value: '#f0ece8', description: 'cream-200 — muted surface for disabled field.' },
  { element: 'opacity (disabled)', token: '--nir-opacity-disabled', value: '0.38', description: 'WCAG standard disabled visual.' },
  { element: 'focus-ring', token: '--nir-color-interaction-focus-ring', value: '#083459', description: '2px solid outline, 2px offset.' },
]} />

## 3. Interactive Playground

<Playground column bg="container">
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1.5rem', width: '100%', maxWidth: '400px' }}>
    <Input client:load label="Full Name" placeholder="Enter your full name" fullWidth />
    <Input client:load label="PAN Card Number" defaultValue="ABCDE1234" error="Must match format: ABCDE1234F" fullWidth />
    <Input client:load label="Mobile Number" disabled defaultValue="+91 98765 43210" fullWidth />
  </div>
</Playground>

## 4. Component Anatomy

```
┌─ label ─────────────────────────────┐
│ Full Name                           │
├─────────────────────────────────────┤
│  Input text or placeholder          │  ← .nir-e-input__field
│                                     │
└─────────────────────────────────────┘
  Helper text or error message          ← .nir-e-input__helper / __error
```

## 5. States Reference

| State | Border token | Background | Description |
| :--- | :--- | :--- | :--- |
| Default | `--nir-colour-border-default` | `--nir-colour-surface-container` | Resting state |
| Focused | `--nir-colour-border-focus` | `--nir-colour-surface-container` | 2px navy inset ring |
| Error | `--nir-colour-border-error` | `--nir-colour-feedback-error-subtle` | Invalid data |
| Success | `--nir-colour-border-success` | `--nir-colour-surface-container` | Validated |
| Disabled | `--nir-colour-border-disabled` | `--nir-colour-interaction-disabled-bg` | 0.38 opacity |

## 6. Engineering Implementation

<Tabs>
  <TabItem label="React">
    ```tsx
    import { Input } from '@nirman/components';

    // All ARIA links, token states, and announcements are automatic
    <Input
      label="Aadhaar ID"
      placeholder="Enter 12-digit number"
      helperText="Find your Aadhaar on your e-Aadhaar card"
      required
      fullWidth
    />

    // Error state — aria-invalid + aria-describedby bound automatically
    <Input
      label="PAN Card Number"
      error="Must match format: ABCDE1234F"
      fullWidth
    />
    ```
  </TabItem>
  <TabItem label="SwiftUI (iOS)">
    ```swift
    import NirmaanTokens

    VStack(alignment: .leading, spacing: NirSpacingVerticalSmallest) {
        Text("Aadhaar ID")
            .font(.custom(NirFontFamilyBody, size: NirFontSizeBodySm))
            .foregroundColor(Color(NirColourTextSecondary))

        TextField("Enter 12-digit number", text: $value)
            .padding(.horizontal, NirSpacingHorizontalBase)
            .padding(.vertical, NirSpacingVerticalSmaller)
            .frame(minHeight: NirSpacingTouchTarget)
            .overlay(
                RoundedRectangle(cornerRadius: NirSpacingCornerRadiusBase)
                    .stroke(
                        hasError ? Color(NirColourBorderError) : Color(NirColourBorderDefault),
                        lineWidth: 2
                    )
            )
            .accessibilityLabel("Aadhaar ID")
            .accessibilityHint(hasError ? "Invalid format" : "Enter 12-digit number")
    }
    ```
  </TabItem>
  <TabItem label="Compose (Android)">
    ```kotlin
    import dev.nirmaan.tokens.NirmaanTheme

    OutlinedTextField(
        value = text,
        onValueChange = { text = it },
        label = { Text("Aadhaar ID") },
        isError = hasError,
        supportingText = {
            if (hasError) Text("Invalid format", color = NirmaanTheme.colors.feedbackErrorText)
        },
        colors = OutlinedTextFieldDefaults.colors(
            focusedBorderColor = NirmaanTheme.colors.borderFocus,
            unfocusedBorderColor = NirmaanTheme.colors.borderDefault,
            errorBorderColor = NirmaanTheme.colors.borderError,
        ),
        modifier = Modifier
            .fillMaxWidth()
            .defaultMinSize(minHeight = NirmaanTheme.spacing.touchTarget)
    )
    ```
  </TabItem>
</Tabs>
