---
title: Input
description: Standard Text inputs mapping foundational interaction bounds.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import Playground from '../../../components/Playground.astro';
import { Input } from '@nirman/components';

Text inputs govern all standard data entry on Web, iOS, and Android seamlessly. Our forms map precisely to `--nir-border-focus` and `--nir-feedback-error-border` Semantic tokens natively.

## Interactive Playground

<Playground>
  <div style={{ display: 'flex', flexDirection: 'column', gap: '1rem', width: '100%' }}>
    <Input client:load label="Email Address" placeholder="alex@example.com" fullWidth />
    <Input client:load label="Username" helperText="You cannot change this later." fullWidth />
    <Input client:load label="Password" error="Password must be 8 characters." fullWidth />
  </div>
</Playground>

<Tabs>
  <TabItem label="React Integration">
    ```tsx
    import { Input } from '@nirman/components';

    // Helper text or Error natively controls ARIA descriptions!
    <Input label="Secure String" error="Invalid parsing format" />
    ```
  </TabItem>
  <TabItem label="SwiftUI Integration">
    ```swift
    // Swift binds explicitly to Tokens!
    TextField("Secure String", text: $text)
      .padding()
      .overlay(
          RoundedRectangle(cornerRadius: Tokens.Radius.md)
              .stroke(Tokens.Color.Border.error, lineWidth: 2)
      )
    ```
  </TabItem>
</Tabs>
